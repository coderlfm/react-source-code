module.exports = {
  // 箭头函数参数括号 默认avoid 可选 avoid| always
  // avoid 能省略括号的时候就省略 例如x => x
  // always 总是有括号
  arrowParens: "avoid",

  // 换行长度，默认80
  printWidth: 120,
  singleQuote: true,
  jsxSingleQuote: true,
};
